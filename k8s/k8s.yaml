apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-material-starter-template
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: angular-material-starter-template
  template:
    metadata:
      labels:
        app: angular-material-starter-template
    spec:
      containers:
        - name: angular-material-starter-template
          image: deepaksorthiya/angular-material-starter-template:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: angular-material-starter-template-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: angular-material-starter-template
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001
